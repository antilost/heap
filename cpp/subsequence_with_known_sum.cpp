/* 
 * https://femida.yandex-team.ru/problems/864
 *
 * Дан массив целых чисел, нужно найти подотрезок (непрерывную подпоследовательность) с заданной суммой X,
 * либо сказать, что это невозможно.
 *
 * Что нужно учитывать:
 *  - В массиве могут быть отрицательные числа.
 *  - Подотрезок должен быть непустой.
 */

#include <iostream>
#include <vector>
#include <map>

using namespace std;

int main(void) {
    // Get vector data
    int vector_length;
    cin >> vector_length;
    vector<int> v(vector_length);
    for (int i = 0; i < vector_length; i++) {
        cin >> v[i];
    }

    // ???
    
    
    
    

    return 0;
}
